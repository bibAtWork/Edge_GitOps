# Copy this directory to cluster/base/applications/<name> before registering it.
# This example is intentionally absent from the live platform composition.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - workload.yaml
  - access.yaml
  - route.yaml
