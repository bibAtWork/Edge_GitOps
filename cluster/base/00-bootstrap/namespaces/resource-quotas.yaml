---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: monitoring
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.memory: 16Gi
    pods: "50"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: seaweedfs
spec:
  # Raised from 8Gi on 2026-08-18. The CSI mount DaemonSet needs a real memory
  # limit (it holds the FUSE daemon for every SeaweedFS PVC on the node, and
  # was OOMKilled at the LimitRange's 256Mi default, taking every mount down
  # with it), and giving it 2Gi put this namespace at exactly 8Gi of 8Gi --
  # 100% consumed, 7 active pods. At the cap nothing new admits, so the very
  # next filer-postgres-backup CronJob run would have failed with "exceeded
  # quota" and the metadata backups would have stopped silently.
  #
  # 12Gi leaves headroom for the backup jobs plus a second mount pod during a
  # DaemonSet recycle. Node has 32Gi allocatable at ~44% use, so this is not
  # over-committing the node -- it is making the quota reflect what the
  # storage layer actually needs.
  hard:
    requests.cpu: "2"
    requests.memory: 8Gi
    limits.memory: 16Gi
    pods: "20"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: velero
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.memory: 2Gi
    pods: "10"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: cert-manager
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: 512Mi
    limits.memory: 1Gi
    pods: "10"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: external-dns
spec:
  hard:
    requests.cpu: "200m"
    requests.memory: 256Mi
    limits.memory: 512Mi
    pods: "5"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: zot
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: 512Mi
    limits.memory: 1Gi
    pods: "5"
---
# Efficiency review 2026-08-17: briefly widened to 4Gi/2Gi/2cpu/15pods here to
# work around scan Jobs getting rejected by this quota. That was treating a
# symptom -- the actual cause was operator.scanJobConcurrentLimit being
# misspelled (real key: scanJobsConcurrentLimit), so trivy-operator ran on the
# chart's default of 10 concurrent scan Jobs instead of the intended 1. Fixed
# properly in 13-trivy-operator/helmrelease.yaml and
# 1-node/patches/trivy-concurrency-1.yaml, and verified live: with concurrency
# genuinely capped at 1, polling every 8s for 2 minutes under the same
# artificial burst that originally broke this quota never showed more than 1
# concurrent scan pod.
#
# Sized down from #160's 4Gi/2Gi/2cpu/15pods once the real cause (above) was
# fixed. scanJobsConcurrentLimit: 1 is enforced per scanner type (verified
# live), not globally, so vulnerabilityScanner/configAuditScanner/
# rbacAssessmentScanner can each have one Job in flight at once -- up to ~3
# concurrent, not a strict 1. At this 2Gi cap that occasionally produced
# rejections under a forced burst deliberately larger than anything
# realistic -- harmless (the operator's reconcile loop retried them and they
# succeeded once earlier Jobs freed memory, zero app impact), and accepted as
# the tradeoff: prioritizing a tight, easy-to-reason-about quota over paying
# for headroom against a burst this large.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: trivy-system
  # Raised 2026-08-25 for the Trivy server added alongside the operator.
  # At 2Gi the namespace held operator (512Mi) + server (512Mi) and could not
  # admit a scan pod, which asks for a 1G limit -- scan Jobs sat in FailedCreate
  # with "exceeded quota" and produced no reports at all. A quota rejection
  # stops pods being created rather than failing them, so nothing crash-looped
  # and nothing appeared in the operator log; the only symptom was reports
  # quietly not being refreshed.
  #
  # 3Gi covers operator + server + one scan pod with margin.
  # scanJobsConcurrentLimit is 2, so two simultaneous scans would still be
  # refused -- deliberately: refusing the second is better than OOM-killing the
  # operator, which currently runs at 499Mi against its own 512Mi limit.
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1536Mi
    limits.memory: 3Gi
    pods: "12"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: tailscale
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: 512Mi
    limits.memory: 1Gi
    pods: "10"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: immich
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.memory: 8Gi
    pods: "20"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: paperless
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.memory: 4Gi
    pods: "10"
