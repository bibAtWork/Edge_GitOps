apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrelease.yaml
  - storageclass.yaml
  # Longhorn's default backup target, kept empty: Longhorn backups ended at the
  # ADR-012 cutover. See the file for why it is not simply removed.
