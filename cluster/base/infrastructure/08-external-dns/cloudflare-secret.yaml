# Encrypt with SOPS before committing
# Reuse the same Cloudflare token as cert-manager (Zone:DNS:Edit + Zone:Zone:Read)
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: external-dns
stringData:
  api-token: REPLACE_WITH_CLOUDFLARE_API_TOKEN
