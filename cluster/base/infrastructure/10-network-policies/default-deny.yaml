---
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: default-deny-ingress
spec:
  # Exclude reserved:ingress — the gateway proxy has its own dedicated ingress policy
  # (allow-gateway-world-ingress). The default-deny must not overlap with it.
  # Note: enforce_policy_on_l7lb stays true for Gateway API listeners regardless of
  # ingress-policy count; the correct fix is an explicit NPDS allowlist, not zero policies.
  endpointSelector:
    matchExpressions:
      - key: reserved.ingress
        operator: DoesNotExist
  ingress:
    - fromEntities:
        - host
---
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: default-deny-egress
spec:
  endpointSelector:
    matchExpressions:
      - key: reserved.ingress
        operator: DoesNotExist
  egress:
    - toEntities:
        - host
        - kube-apiserver
