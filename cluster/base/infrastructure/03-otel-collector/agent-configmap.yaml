apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-agent-config
  namespace: monitoring-agents
data:
  config.yaml: |
    receivers:
      hostmetrics:
        collection_interval: 30s
        scrapers:
          cpu: {}
          memory: {}
          disk: {}
          network: {}
          filesystem: {}
          load: {}
      filelog:
        include:
          - /var/log/pods/*/*/*.log
        include_file_path: true
        operators:
          - type: regex_parser
            parse_from: attributes["log.file.path"]
            regex: '^/var/log/pods/(?P<kubernetes_namespace_name>[^_/]+)_(?P<kubernetes_pod_name>[^_/]+)_[^/]+/(?P<kubernetes_container_name>[^/]+)/'
          - type: json_parser
            if: 'body matches "^{"'
          - type: regex_parser
            regex: '^(?P<time>[^ ^Z]+Z) (?P<stream>stdout|stderr) (?P<logtag>[^ ]*) ?(?P<log>.*)$'
            if: 'body matches "^[^ ]+ (stdout|stderr)"'
            timestamp:
              parse_from: attributes.time
              layout: '%Y-%m-%dT%H:%M:%S.%LZ'
      filelog/audit:
        include:
          - /var/log/kubernetes/audit/audit.log
        include_file_path: true
        operators:
          - type: json_parser
            timestamp:
              parse_from: attributes.requestReceivedTimestamp
              layout_type: strptime
              layout: '%Y-%m-%dT%H:%M:%S.%fZ'
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318
      prometheus:
        config:
          scrape_configs:
            - job_name: "cilium-hubble"
              static_configs:
                - targets: ["hubble-metrics.kube-system.svc:9965"]

    processors:
      batch:
        timeout: 10s
        send_batch_size: 1024
      memory_limiter:
        limit_mib: 200
        spike_limit_mib: 50
        check_interval: 5s
      resource:
        attributes:
          - action: insert
            key: cluster
            value: homelab

    exporters:
      prometheusremotewrite:
        endpoint: http://vmsingle-vmstack-victoria-metrics-k8s-stack.monitoring.svc:8428/api/v1/write
      otlp/gateway:
        endpoint: otel-collector-gateway.monitoring.svc:4317
        tls:
          insecure: true

    service:
      pipelines:
        metrics:
          receivers: [hostmetrics, prometheus]
          processors: [memory_limiter, batch, resource]
          exporters: [prometheusremotewrite]
        logs:
          receivers: [filelog, filelog/audit]
          processors: [memory_limiter, batch, resource]
          exporters: [otlp/gateway]
        traces:
          receivers: [otlp]
          processors: [memory_limiter, batch, resource]
          exporters: [otlp/gateway]
