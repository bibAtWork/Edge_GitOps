# Encrypt with SOPS before committing
# Token requires Zone:DNS:Edit + Zone:Zone:Read scoped to your domain only
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
stringData:
  api-token: REPLACE_WITH_CLOUDFLARE_API_TOKEN
