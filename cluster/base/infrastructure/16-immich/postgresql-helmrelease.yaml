apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich-postgresql
  namespace: immich
spec:
  interval: 30m
  chart:
    spec:
      chart: postgresql
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
      interval: 12h
  install:
    timeout: 10m
    remediation:
      retries: 3
  upgrade:
    timeout: 10m
    remediation:
      retries: 3
      remediateLastFailure: true
    cleanupOnFail: true
  values:
    global:
      # Immich ships its own postgres image with pgvecto.rs; bitnami chart 18.x+
      # requires this flag to allow non-bitnami container images.
      security:
        allowInsecureImages: true

    # Immich requires PostgreSQL with pgvecto.rs; use the official Immich image.
    image:
      registry: ghcr.io
      repository: immich-app/postgres
      tag: 17-vectorchord0.3.0-pgvectors0.3.0

    auth:
      username: immich
      database: immich
      existingSecret: immich-db-secret
      secretKeys:
        userPasswordKey: db-password
        adminPasswordKey: db-admin-password

    primary:
      # The immich postgres image writes to /etc/postgresql during init;
      # bitnami chart 18.x sets readOnlyRootFilesystem: true by default.
      containerSecurityContext:
        readOnlyRootFilesystem: false
      # pgvecto.rs vector extension requires more memory than bitnami's 192Mi default.
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          memory: 1Gi
      persistence:
        # longhorn-db, not local-path. This database keeps its purpose-built
        # image (vchord + vector extensions for CLIP embedding search), so it is
        # NOT migrated to CloudNativePG -- CNPG ships no image carrying those,
        # and a stock image would import cleanly then silently break semantic
        # search. Only the storage moves: off hostPath onto a real block device
        # with ext4, which is what made the fsync-durability question go away
        # and what makes volume-level backup possible at all.
        #
        # Retain reclaim policy, so a stray PVC delete cannot destroy PGDATA.
        storageClass: longhorn-db
        accessMode: ReadWriteOnce
        size: 10Gi
        labels:
          velero.io/backup: "true"

    metrics:
      enabled: false
