apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  # secret.yaml (dex-secrets): create and SOPS-encrypt manually before merging.
  # See PR #83 description for the required Secret format (name: dex-secrets).
  - cilium-policy.yaml
