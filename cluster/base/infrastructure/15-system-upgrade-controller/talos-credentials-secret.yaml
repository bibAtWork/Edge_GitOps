# Encrypt with SOPS before committing.
# Generate via: talosctl config export --output talosconfig
# This allows the upgrade job to call the Talos API.
apiVersion: v1
kind: Secret
metadata:
  name: talos-credentials
  namespace: system-upgrade
stringData:
  talosconfig: |
    REPLACE_WITH_BASE64_ENCODED_TALOSCONFIG
